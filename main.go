package main

import "fmt"

func main() {
  var publisher, writer, artist, title string
  var year, pageNumber uint
  var grade float32

  fmt.Println("Publisher:", publisher, "Writer:", writer, "Artist:", artist, "Title:",
	title, "Year:", year, "Page Number:", pageNumber, "Grade:", grade)
}